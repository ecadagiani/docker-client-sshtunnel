# docker-client-sshtunnel
A docker, to expose your localhost to the web, through a ssh server

To see how to implement, see "example" directory

The server used is https://github.com/antoniomika/sish 